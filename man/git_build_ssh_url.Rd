% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/git_helpers.R
\name{git_build_ssh_url}
\alias{git_build_ssh_url}
\title{Build an SSH remote URL for a repository}
\usage{
git_build_ssh_url(repo, host = "github.com", ssh_alias = NULL)
}
\arguments{
\item{repo}{Character. Repository identifier. Accepts either:
\itemize{
\item a short slug, \code{"owner/repo"} (most common case), or
\item a full SSH URL, \code{"git@host:owner/repo.git"}, or
\item a full HTTPS URL, \code{"https://host/owner/repo.git"} (automatically
converted to SSH so the configured alias/keys are used).
}}

\item{host}{Character. Git hosting domain, used only when \code{repo} is a
short slug. Defaults to \code{"github.com"}.}

\item{ssh_alias}{Character or \code{NULL}. SSH \code{Host} alias configured in
\verb{~/.ssh/config} for the desired account (e.g. \code{"github.com-work"}). When
supplied, it replaces \code{host} in the SSH remote so Git/SSH pick the right
identity file automatically. When \code{NULL} (default), \code{host} is used
directly, which works fine for a single-account setup or when the
default SSH identity already has access.}
}
\value{
Character. A single SSH remote URL, e.g.
\code{"git@github.com-work:owner/repo.git"}.
}
\description{
Internal helper used by \code{\link[=git_clone]{git_clone()}} (and reusable by other \verb{git_*}
helpers) to normalize a repository identifier into a proper SSH remote
URL, optionally substituting an SSH host alias in place of the real host
so that \code{ssh}/\code{git} resolve the correct identity file for multi-account
setups.
}
\keyword{internal}
