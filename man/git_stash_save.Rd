% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/git_helpers.R
\name{git_stash_save}
\alias{git_stash_save}
\title{Save (Stash) Uncommitted Local Changes}
\usage{
git_stash_save(message_text = NULL, include_untracked = TRUE)
}
\arguments{
\item{message_text}{Character or \code{NULL}. Description recorded with
the stash entry. When \code{NULL} (default), a timestamped message is
generated automatically.}

\item{include_untracked}{Logical. If \code{TRUE} (default), passes
\code{-u} so untracked files are stashed too, not just modifications to
already-tracked files.}
}
\value{
Invisibly returns \code{TRUE} if a stash entry was created, or
\code{FALSE} if there was nothing to stash.
}
\description{
Wrapper around \code{git stash push}, used internally as the safety-net
step of \code{\link{git_pull_force_remote}} before a hard reset. Cleanly
no-ops (with a message, no error) when there is nothing to stash, so it is
safe to call unconditionally.
}
\examples{
\dontrun{
git_stash_save("wip: refactor before pulling")
}
}
\seealso{
\code{\link{git_stash_list}}, \code{\link{git_stash_pop}}
}
