% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/git_helpers.R
\name{git_clone}
\alias{git_clone}
\title{Clone a Git repository using an SSH host alias}
\usage{
git_clone(
  repo,
  dest_dir = NULL,
  host = "github.com",
  ssh_alias = NULL,
  branch = NULL,
  depth = NULL,
  overwrite = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{repo}{Character. Repository identifier. Accepts either:
\itemize{
\item a short slug, \code{"owner/repo"} (most common case), or
\item a full SSH URL, \code{"git@host:owner/repo.git"}, or
\item a full HTTPS URL, \code{"https://host/owner/repo.git"} (automatically
converted to SSH so the configured alias/keys are used).
}}

\item{dest_dir}{Character. Destination directory for the clone. Defaults to
the repository name (i.e. what \verb{git clone} would use on its own).}

\item{host}{Character. Git hosting domain, used only when \code{repo} is a
short slug. Defaults to \code{"github.com"}.}

\item{ssh_alias}{Character or \code{NULL}. SSH \code{Host} alias configured in
\verb{~/.ssh/config} for the desired account (e.g. \code{"github.com-work"}). When
supplied, it replaces \code{host} in the SSH remote so Git/SSH pick the right
identity file automatically. When \code{NULL} (default), \code{host} is used
directly, which works fine for a single-account setup or when the
default SSH identity already has access.}

\item{branch}{Character or \code{NULL}. If supplied, passed to \verb{git clone} as
\verb{--branch <branch>}.}

\item{depth}{Integer or \code{NULL}. If supplied, passed to \verb{git clone} as
\verb{--depth <depth>} for a shallow clone.}

\item{overwrite}{Logical. If \code{TRUE} and \code{dest_dir} already exists, it is
removed before cloning. Defaults to \code{FALSE}.}

\item{quiet}{Logical. If \code{TRUE}, suppresses \code{git}'s own progress output
(still returns an error on failure). Defaults to \code{FALSE}.}
}
\value{
Invisibly, the normalized path to the cloned repository. Stops
with an informative error if \code{git} is not available, if the alias is not
found in \verb{~/.ssh/config}, or if \verb{git clone} itself fails.
}
\description{
Generic, cross-platform wrapper around \verb{git clone} that builds the correct
SSH remote URL from a repository slug (\code{"owner/repo"}), an optional SSH
host alias, and an optional custom Git host. Credential resolution is fully
delegated to the SSH layer (via \verb{~/.ssh/config}), matching the multi-account
pattern already used elsewhere in this package (see \code{git_pull_force_remote()}
and the other \verb{git_*} helpers) instead of embedding tokens or passwords in R.
}
\details{
Any user can rely on this function regardless of how many GitHub/GitLab
accounts they manage: as long as the relevant \code{Host} alias is set up in
\verb{~/.ssh/config}, \code{git_clone()} takes care of building the right remote URL
and calling \verb{git clone}.
}
\examples{
\dontrun{
# Simple clone using the default SSH identity
git_clone("tidyverse/dplyr")

# Clone using a specific SSH host alias (multi-account setup)
git_clone("my-org/private-repo", ssh_alias = "github.com-work")

# Shallow clone of a specific branch into a custom directory
git_clone("my-account/my-package", dest_dir = "my-package-dev",
          branch = "develop", depth = 1)
}
}
